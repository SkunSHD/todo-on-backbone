module.exports = function() {
    // var sum = 0, i = 0, args = arguments, l = args.length;
    // while (i < l) {
    //     sum += args[i++];
    // }
    // return sum;

    var privat = 'secret',
    sum = arguments[0] + arguments[1];
    return sum;
};